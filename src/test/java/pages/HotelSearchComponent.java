package pages;

import org.openqa.selenium.WebDriver;

public class HotelSearchComponent extends BasePage{

    public HotelSearchComponent(WebDriver webDriver)
    {
        super(webDriver);
    }
}
